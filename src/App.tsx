import React from 'react';
import * as Styled from './App.styled';

const App: React.FC = () => {
    return (
        <Styled.AppContainer>
            <h1>Hello Snowpack World!</h1>
        </Styled.AppContainer>
    );
};

export default App;
